# Synthetic development data

Run `python ../../scripts/generate_development_data.py` from this directory, or run the script from `ai/`, to create deterministic synthetic sessions. Generated CSVs are ignored and are not research evidence.
